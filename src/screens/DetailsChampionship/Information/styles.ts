import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    gap: 13,
    flex: 1,
  },
  flat: {
    paddingTop: 30,
    paddingBottom: 30,
  },
  contentScroll: {
    gap: 15
  }
})